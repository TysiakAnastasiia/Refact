# Лабораторна робота 4

## ПЗ з використанням шаблонів проектування

### Опис проєкту

Цей проєкт реалізує систему управління замовленнями в ресторані з використанням принципів SOLID, підходу TDD (Test-Driven Development) та шаблонів проєктування.

Система дозволяє:

- створювати та керувати меню
- додавати страви
- створювати замовлення
- прив’язувати замовлення до клієнта
- сповіщати кухню про нові замовлення

---

## Архітектура системи

### Основні класи:

- `Dish` — представляє страву
- `Menu` — колекція страв
- `Customer` — клієнт
- `Order` — замовлення
- `KitchenNotifier` — оповіщення кухні

---

## Дотримання SOLID

- **SRP (Single Responsibility Principle)**  
  Кожен клас відповідає за одну логіку (наприклад, `Dish` — тільки дані страви)

- **OCP (Open/Closed Principle)**  
  Нові типи замовлень додаються через фабрики без зміни існуючого коду

- **DIP (Dependency Inversion Principle)**  
  Використано абстракції (інтерфейси) для зменшення залежностей між компонентами

---

## TDD підхід

Розробка функціоналу проводилась через написання тестів перед реалізацією.

Реалізовані сценарії:

- додавання страв у меню
- створення замовлення
- асоціація замовлення з клієнтом
- обробка помилок (порожнє меню, некоректні дані)

---

## Шаблони проєктування

### 1. Singleton

`OrderDatabase`

- гарантує один екземпляр бази даних замовлень

### 2. Factory

- `RegularOrderFactory`
- `BulkOrderFactory`
- `OrderFactoryProvider`

Використовується для створення різних типів замовлень

### 3. Observer

`KitchenNotifier`

- сповіщає кухню про нові замовлення

---

## Тестування

Файл: `tests/test_all.py`

Реалізовано 49 модульних тестів, які покривають:

- створення об'єктів (Dish, Menu, Customer, Order)
- обробку помилок (валідація)
- взаємодію між компонентами
- роботу шаблонів:
  - Singleton (OrderDatabase)
  - Factory (створення замовлень)
  - Observer (KitchenNotifier)

Тести також включають повні сценарії роботи системи (end-to-end flow).

---

## UML-діаграма

UML-діаграма класів відображає структуру системи, зв’язки між класами та використані шаблони проєктування.

Файл: `docs/uml.png`

## Структура проєкту

src/
│
├── models/
│ ├── dish.py # Клас Dish — модель страви
│ ├── menu.py # Клас Menu — управління списком страв
│ ├── customer.py # Клас Customer — модель клієнта
│ └── order.py # Клас Order — логіка замовлення
│
├── patterns/
│ ├── order_database.py # Singleton — база даних замовлень
│ ├── notifier.py # Observer — KitchenNotifier
│ ├── factories.py # Factory — створення замовлень
│ └── **init**.py
│
├── interfaces/
│ └── (інтерфейси для DIP)
│
tests/
│ └── test_all.py # 49 модульних тестів
docs/
│ └── uml.png # UML-діаграма класів системи

## Висновок

У ході виконання лабораторної роботи було спроєктовано та реалізовано систему управління замовленнями в ресторані.

Було застосовано принципи SOLID, що дозволило створити гнучку та розширювану архітектуру. Використання підходу TDD забезпечило надійність коду та покриття основного функціоналу тестами.

Також було реалізовано шаблони проєктування:

- Singleton — для управління базою замовлень
- Factory — для створення різних типів замовлень
- Observer — для сповіщення кухні
